package ga;

import com.google.android.gms.common.api.Status;
import d.m0;
import e8.a;
import g8.o;

@a
public class c implements o {
    @m0
    public final Exception a(@m0 Status status) {
        return status.f1() == 8 ? new b(status.F1()) : new a(status.F1());
    }
}
