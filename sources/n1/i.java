package n1;

import android.widget.CheckedTextView;
import androidx.databinding.g;
import androidx.databinding.h;
import d.x0;

@x0({x0.a.LIBRARY})
@h({@g(attribute = "android:checkMark", method = "setCheckMarkDrawable", type = CheckedTextView.class), @g(attribute = "android:checkMarkTint", method = "setCheckMarkTintList", type = CheckedTextView.class)})
public class i {
}
