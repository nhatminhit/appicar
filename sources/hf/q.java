package hf;

import fh.g;
import fh.h;
import hf.o;
import we.l;
import zd.h1;

public interface q<T, V> extends o<V>, l<T, V> {

    public interface a<T, V> extends o.c<V>, l<T, V> {
    }

    @g
    a<T, V> a();

    V get(T t10);

    @h1(version = "1.1")
    @h
    Object q(T t10);
}
